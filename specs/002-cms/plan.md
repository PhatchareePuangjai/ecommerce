# Implementation Plan: CMS

**Branch**: `002-cms` | **Date**: 2025-10-10 | **Spec**: /Users/toy/Desktop/learn/AI/demo-api/specs/002-cms/spec.md
**Input**: Feature specification from `/specs/002-cms/spec.md` (derived from umbrella)

## Execution Flow (/plan command scope)
```
1. Load feature spec from Input path
   → If not found: ERROR "No feature spec at {path}"
2. Fill Technical Context (scan for NEEDS CLARIFICATION)
   → Detect Project Type from repo context (backend API)
   → Set Structure Decision based on project type
3. Fill the Constitution Check section based on the umbrella constitution document.
4. Evaluate Constitution Check section below
   → If violations exist: Document in Complexity Tracking
   → If no justification possible: ERROR "Simplify approach first"
   → Update Progress Tracking: Initial Constitution Check
5. Execute Phase 0 → research items specific to CMS
6. Execute Phase 1 → derive data model, contracts, quickstart
7. Re-evaluate Constitution Check section
   → If new violations: Refactor design, return to Phase 1
   → Update Progress Tracking: Post-Design Constitution Check
8. Plan Phase 2 → Describe task generation approach (DO NOT create tasks.md)
9. STOP - Ready for /tasks command
```

**IMPORTANT**: The /plan command STOPS at step 7. Phases 2-4 are executed by other commands:
- Phase 2: /tasks command creates tasks.md
- Phase 3-4: Implementation execution (manual or via tools)

## Summary
CMS feature enabling content authoring and presentation controls for site themes, banners, and articles/news with preview and scheduling. Follows umbrella defaults for preview access controls and propagation SLAs (≤ 5 minutes) and adds explicit monitoring/alert requirements. Scope excludes product/catalog and checkout logic. Supports workflow Draft → Review → Approved → Published with optional Archive, audit trail retention ≥18 months, and scheduling windows (UTC storage, locale-aware display). Banner ordering and overlapping schedules resolve by most recent publish time with `order` and stable ids as tie-breakers. Admins are the sole CMS operators; Visitors/Users consume published content only.

## Technical Context
**Language/Version**: Node.js 20.x  
**Primary Dependencies**: Express 4.x (API), Jest 29.x (tests)  
**Storage**: NEEDS CLARIFICATION (e.g., Postgres tables for content, banners, themes; audit logs)  
**Auth/Permissions**: Depends on Auth/Profile epic (009) for roles and preview access  
**Testing**: TDD-first per Constitution  
**Constraints**: Preview visible only to configured preview roles; propagation ≤ 5 minutes; schedule stored in UTC  
**Project Type**: Backend API within existing repo  

Technical context from /plan arguments: None provided

## Constitution Check
This plan aligns with umbrella principles:
- Test-first orientation (contracts + failing tests planned for later execution)
- Observability/performance adherence via umbrella defaults (propagation windows)
- Simplicity: focus CMS scope; defer auth specifics to 009-auth-profile

Initial Constitution Check: PASS

## Project Structure

### Documentation (this feature)
```
specs/002-cms/
├── plan.md              # This file (/plan command output)
└── spec.md              # Provided feature specification
```

### Source Code (repository root)
Planned additions in subsequent phases (generated by /tasks and implementation):
```
src/
├── api/routes/cms/*           # CMS routes (articles, banners, themes, preview)
├── models/cms/*               # Content, Banner, Theme models
└── services/cms/*             # Workflow, scheduling, preview services
```

## Phase 0: Outline & Research
1. Identify unknowns from spec:
   - Roles/permissions for preview access → depends on 009-auth-profile
   - Audit trail storage strategy (separate table vs JSONB audit column)
   - Scheduling evaluation strategy (on-read filter vs background scheduler)
2. Document decisions and trade-offs in a research note.

Output: research.md (CMS-specific) with critical NEEDS CLARIFICATION resolved or deferred to dependencies

## Phase 1: Design & Contracts
Prerequisites: research complete

1. Define CMS entities from spec → `data-model.md` (Content, Banner, Theme; workflow states; schedule windows; audit trail).
2. Generate API contracts for CMS endpoints (admin and read-only public):
   - `POST/PUT/GET /cms/articles` (CRUD, workflow transitions, preview)
   - `POST/PUT/GET /cms/banners` (CRUD, ordering, scheduling)
   - `GET /cms/preview/:type/:id` (authorized preview)
   - Note: Secure endpoints require role checks (integration with 009)
3. Produce contract tests (failing initially) from OpenAPI.
4. Draft `quickstart.md` scenarios for authoring, preview, scheduling.

Output: data-model.md, contracts/* (skeleton), quickstart.md (all CMS-scoped)

## Phase 2: Task Planning Approach
Describe what /tasks will generate (do not execute here):

Task Generation Strategy:
- Each contract endpoint → contract test task [P]
- Each entity → model creation task [P]
- Workflow + scheduling services → service tasks with unit tests
- Admin vs public endpoints → separate implementation tasks

Ordering Strategy:
- TDD-first: contract tests before implementation
- Models → services → endpoints → integration tests
- [P] for independent files; serialize where files overlap

Estimated Output: 20–30 numbered, ordered tasks in tasks.md

## Phase 3+: Future Implementation
Phase 3: /tasks creates tasks.md  
Phase 4: Implementation to satisfy tests  
Phase 5: Validation (run tests, verify propagation/preview constraints)

## Complexity Tracking
Fill if Constitution Check has violations requiring justification

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|

## Progress Tracking

**Phase Status**:
- [x] Phase 0: Research outlined (this plan)
- [x] Phase 1: Design approach defined (this plan)
- [ ] Phase 2: Task planning complete (/tasks command)
- [ ] Phase 3: Tasks generated (/tasks command)
- [ ] Phase 4: Implementation complete
- [ ] Phase 5: Validation passed

**Gate Status**:
- [x] Initial Constitution Check: PASS
- [ ] Post-Design Constitution Check: Pending
- [ ] All NEEDS CLARIFICATION resolved (critical)
- [ ] Complexity deviations documented

---
Based on Constitution (see `.specify/memory/constitution.md`) and umbrella defaults
