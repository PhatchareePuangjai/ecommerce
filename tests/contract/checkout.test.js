describe('POST /checkout contract', () => {
  test('fails with 422 on postal validation mismatch', () => {
    // TODO: Assert 422 for invalid postal address; schema per contracts/openapi.yaml
  });
});

